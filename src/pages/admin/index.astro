---
// Admin Dashboard - モード選択画面
---
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>管理ダッシュボード | BLITZ</title>
  <link rel="icon" type="image/png" href="/favicon.png" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      font-family: 'Inter', sans-serif;
      background: #0a0a0f;
      color: #fff;
      min-height: 100vh;
    }

    .admin-bg {
      position: fixed;
      inset: 0;
      background:
        radial-gradient(ellipse at 30% 20%, rgba(0, 212, 255, 0.06) 0%, transparent 50%),
        radial-gradient(ellipse at 70% 80%, rgba(78, 205, 196, 0.04) 0%, transparent 50%),
        #0a0a0f;
      z-index: 0;
    }

    .admin-wrapper {
      position: relative;
      z-index: 1;
      max-width: 900px;
      margin: 0 auto;
      padding: 2rem 1.5rem;
    }

    .admin-header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 3rem;
      padding-bottom: 1.5rem;
      border-bottom: 1px solid rgba(255,255,255,0.08);
    }

    .admin-logo {
      font-size: 1.5rem;
      font-weight: 700;
      letter-spacing: -0.02em;
    }
    .admin-logo .dot { color: #00D4FF; }
    .admin-logo .badge {
      display: inline-block;
      background: rgba(0, 212, 255, 0.15);
      color: #00D4FF;
      font-size: 0.65rem;
      font-weight: 600;
      padding: 0.2rem 0.5rem;
      border-radius: 99px;
      margin-left: 0.5rem;
      vertical-align: middle;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .header-actions {
      display: flex;
      gap: 0.75rem;
      align-items: center;
    }

    .settings-btn, .logout-btn {
      padding: 0.5rem 1rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 0.5rem;
      color: rgba(255,255,255,0.6);
      font-size: 0.8rem;
      font-family: 'Inter', sans-serif;
      cursor: pointer;
      transition: all 0.2s;
      text-decoration: none;
    }
    .settings-btn:hover, .logout-btn:hover {
      background: rgba(255,255,255,0.1);
      color: #fff;
    }

    .welcome-section {
      text-align: center;
      margin-bottom: 3rem;
    }
    .welcome-section h1 {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }
    .welcome-section p {
      color: rgba(255,255,255,0.5);
      font-size: 0.95rem;
    }

    .mode-cards {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 1.25rem;
      margin-bottom: 3rem;
    }

    .mode-card {
      background: rgba(18, 18, 26, 0.8);
      backdrop-filter: blur(15px);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 1.25rem;
      padding: 2.5rem 2rem;
      cursor: pointer;
      transition: all 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      text-decoration: none;
      color: inherit;
      display: block;
      position: relative;
      overflow: hidden;
    }

    .mode-card::before {
      content: '';
      position: absolute;
      inset: 0;
      border-radius: 1.25rem;
      opacity: 0;
      transition: opacity 0.3s;
    }

    .mode-card.bulk::before {
      background: linear-gradient(135deg, rgba(0, 212, 255, 0.08) 0%, rgba(0, 102, 255, 0.05) 100%);
    }

    .mode-card.create::before {
      background: linear-gradient(135deg, rgba(78, 205, 196, 0.08) 0%, rgba(0, 176, 155, 0.05) 100%);
    }

    .mode-card:hover {
      transform: translateY(-6px);
      border-color: rgba(255,255,255,0.15);
      box-shadow: 0 20px 50px rgba(0,0,0,0.4);
    }

    .mode-card:hover::before {
      opacity: 1;
    }

    .mode-icon {
      font-size: 2.5rem;
      margin-bottom: 1.25rem;
      display: block;
    }

    .mode-card h2 {
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 0.75rem;
      position: relative;
      z-index: 1;
    }

    .mode-card p {
      color: rgba(255,255,255,0.5);
      font-size: 0.875rem;
      line-height: 1.6;
      position: relative;
      z-index: 1;
      margin: 0;
    }

    .mode-tag {
      display: inline-block;
      margin-top: 1.25rem;
      padding: 0.3rem 0.8rem;
      border-radius: 99px;
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      position: relative;
      z-index: 1;
    }

    .bulk .mode-tag {
      background: rgba(0, 212, 255, 0.15);
      color: #00D4FF;
    }

    .create .mode-tag {
      background: rgba(78, 205, 196, 0.15);
      color: #4ECDC4;
    }

    .ai-mode::before {
      background: linear-gradient(135deg, rgba(139, 92, 246, 0.08) 0%, rgba(109, 40, 217, 0.05) 100%);
    }
    .ai-mode .mode-tag {
      background: rgba(139, 92, 246, 0.15);
      color: #8B5CF6;
    }

    /* Settings Modal */
    .modal-overlay {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,0.7);
      backdrop-filter: blur(5px);
      z-index: 999;
      align-items: center;
      justify-content: center;
    }
    .modal-overlay.visible { display: flex; }

    .modal {
      background: #12121a;
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 1rem;
      padding: 2rem;
      width: 90%;
      max-width: 500px;
      animation: cardIn 0.3s ease forwards;
    }

    @keyframes cardIn {
      from { opacity: 0; transform: scale(0.95) translateY(10px); }
      to { opacity: 1; transform: scale(1) translateY(0); }
    }

    .modal h3 {
      font-size: 1.2rem;
      margin-bottom: 1.25rem;
    }

    .modal label {
      display: block;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.06em;
      color: rgba(255,255,255,0.5);
      margin-bottom: 0.4rem;
    }

    .modal input {
      width: 100%;
      padding: 0.75rem 1rem;
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 0.5rem;
      color: #fff;
      font-size: 0.9rem;
      font-family: 'Inter', sans-serif;
      outline: none;
      margin-bottom: 1rem;
      transition: border-color 0.2s;
    }
    .modal input:focus { border-color: #00D4FF; }

    .modal-actions {
      display: flex;
      gap: 0.75rem;
      justify-content: flex-end;
      margin-top: 0.5rem;
    }

    .modal-btn {
      padding: 0.6rem 1.2rem;
      border-radius: 0.5rem;
      font-size: 0.85rem;
      font-family: 'Inter', sans-serif;
      font-weight: 600;
      cursor: pointer;
      border: none;
      transition: all 0.2s;
    }

    .modal-btn.cancel {
      background: rgba(255,255,255,0.05);
      color: rgba(255,255,255,0.6);
      border: 1px solid rgba(255,255,255,0.1);
    }

    .modal-btn.save {
      background: linear-gradient(135deg, #00D4FF, #0066FF);
      color: #fff;
    }
    .modal-btn.save:hover {
      box-shadow: 0 4px 15px rgba(0, 212, 255, 0.3);
    }

    .saved-toast {
      position: fixed;
      bottom: 2rem;
      left: 50%;
      transform: translateX(-50%) translateY(100px);
      background: #1a1a25;
      border: 1px solid rgba(0, 212, 255, 0.3);
      color: #00D4FF;
      padding: 0.75rem 1.5rem;
      border-radius: 0.75rem;
      font-size: 0.85rem;
      font-weight: 600;
      transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
      z-index: 1000;
    }
    .saved-toast.visible {
      transform: translateX(-50%) translateY(0);
    }

    @media (max-width: 900px) {
      .mode-cards { grid-template-columns: 1fr; }
    }
    @media (max-width: 640px) {
      .admin-header { flex-direction: column; gap: 1rem; align-items: flex-start; }
    }
  </style>
</head>
<body>
  <div class="admin-bg"></div>
  <div class="admin-wrapper">
    <header class="admin-header">
      <div class="admin-logo">
        BLITZ<span class="dot">.</span>
        <span class="badge">Admin</span>
      </div>
      <div class="header-actions">
        <button class="settings-btn" id="settingsBtn">⚙️ 設定</button>
        <button class="logout-btn" id="logoutBtn">ログアウト</button>
      </div>
    </header>

    <section class="welcome-section">
      <h1>記事管理</h1>
      <p>新しい記事を作成して公開しましょう</p>
    </section>

    <div class="mode-cards">
      <a href="/admin/bulk" class="mode-card bulk">
        <span class="mode-icon">🚀</span>
        <h2>一括ペーストモード</h2>
        <p>AIで作成した日本・US・インド用の記事を3つ同時にペーストして公開</p>
        <span class="mode-tag">3ヵ国同時投稿</span>
      </a>

      <a href="/admin/create" class="mode-card create">
        <span class="mode-icon">✍️</span>
        <h2>個別作成モード</h2>
        <p>国ごとにタブを切り替えて、1つずつ記事を手動で作成・公開</p>
        <span class="mode-tag">手動エディタ</span>
      </a>

      <a href="/admin/ai" class="mode-card ai-mode">
        <span class="mode-icon">🤖</span>
        <h2>AI生成モード</h2>
        <p>キーワードを入力するだけで3ヵ国分の記事をAIが自動生成</p>
        <span class="mode-tag">📱 スマホ対応</span>
      </a>
    </div>
  </div>

  <!-- Settings Modal -->
  <div class="modal-overlay" id="settingsModal">
    <div class="modal">
      <h3>⚙️ 設定</h3>
      <label for="ghToken">GitHub Personal Access Token</label>
      <input type="password" id="ghToken" placeholder="ghp_xxxxxxxxxxxx" />
      <p style="font-size:0.75rem;color:rgba(255,255,255,0.35);margin-top:-0.5rem;margin-bottom:1rem;">
        記事をGitHubにコミットするために必要です。<a href="https://github.com/settings/tokens" target="_blank" style="color:#00D4FF;">トークンを生成</a>
      </p>
      <div class="modal-actions">
        <button class="modal-btn cancel" id="cancelSettings">キャンセル</button>
        <button class="modal-btn save" id="saveSettings">保存</button>
      </div>
    </div>
  </div>

  <div class="saved-toast" id="savedToast">✅ 設定を保存しました</div>

  <script>
    // Auth check
    if (sessionStorage.getItem('blitz_admin_auth') !== 'true') {
      window.location.href = '/admin/login';
    }

    // Logout
    document.getElementById('logoutBtn').addEventListener('click', () => {
      sessionStorage.removeItem('blitz_admin_auth');
      window.location.href = '/admin/login';
    });

    // Settings modal
    const modal = document.getElementById('settingsModal');
    const tokenInput = document.getElementById('ghToken');

    // Load saved token
    tokenInput.value = localStorage.getItem('blitz_gh_token') || '';

    document.getElementById('settingsBtn').addEventListener('click', () => {
      modal.classList.add('visible');
    });

    document.getElementById('cancelSettings').addEventListener('click', () => {
      modal.classList.remove('visible');
    });

    modal.addEventListener('click', (e) => {
      if (e.target === modal) modal.classList.remove('visible');
    });

    document.getElementById('saveSettings').addEventListener('click', () => {
      localStorage.setItem('blitz_gh_token', tokenInput.value);
      modal.classList.remove('visible');

      const toast = document.getElementById('savedToast');
      toast.classList.add('visible');
      setTimeout(() => toast.classList.remove('visible'), 2500);
    });
  </script>
</body>
</html>
